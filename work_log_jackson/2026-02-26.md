## Log
- Made BatchClient to get rid of complex BatchRequest implementation
- Replaced most of PulseCountClient with BatchClient
- Replaced most of VarMaxClient with BatchClient
- Wrote patches for varmax hardware, for now copied most of pulse count tests
- Made uploadstate, mostly identical to what pulsecountstate was but allows for custom output parsing
- Moved pulsecount and varmax state to use uploadstate 
- Added patches for uploadstate during testing
	- Not really happy with how these turned out since they involve having an additional parameter in these classes and a argument in uploadstate during initialization. Since the pyserial.Serial needs to be patched differently depending on whether uploadstate is initialized for pulsecount or varmax, I wasn't able to patch the library itself like I was doing previously - I would need to manage to patch the import separately in different files. This type of thing is normally possible with pytest, but the worker doesn't run through pytest, it runs normally while the performs the tests
## Next
- Waveform sending with uploadstate
	- Use arg to determine parse function
	- Don't want to just always send because sample size could be large, while this would probably be the ideal design
	- Reformat sent data to (uid, (fitness))/(uid, (fitness, voltages)) so works nicely with batchcilent

[[work_log_jackson/2026-02-25|prev]] [[work_log_jackson/2026-02-27|next]]
