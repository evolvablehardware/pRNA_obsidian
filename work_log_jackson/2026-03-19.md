## Log
- [ ]
## Next
- icefarm setup stuff
	-Localhost with yaml issue (dynamic ip fixes this issue)
	- Normal setup needs arm-none-eabi
	- Make clients unreserve on others ints than sig
		- Vscode python debugger uses sigkill by default, doesn't seem like this is configurable. Still can catch some other signals to make this work better, add instructions for configuring debugger to use sigint or term instead
		- Really should do something about this, will become a greater inconvenience once using a larger system
		- I don't want to end reservations immediately on client disconnects because this could create issues for longer reservations
		- Probably want to decrease reservation to expire after about 5-10m of inactivity rather than an hour
		- Need to make client names easier to identify in the future. Probably should include hostname or user automatically as a default parameter
- Put tile next to genome and next to io pin, use global nets instead. This is sooo much easier than messing with the neigh net chain and will work across ram tiles
- Go through TODOs, there's more than 10
- Make batchclient gracefully handle devices that are rebooting by not sending any new bitstreams
- Improve design of BatchClient after disconnect detection changes
- Min amount of devices for experiment continuation when they fail
- Add reboot amout of tries to uplooadstate config
- Review stability changes, ensure that client has failsafe for detecting worker timeouts instead of only relying on control notification
- Fix db connection in workflow tests
- Dynamic worker ip
- Apparently queue.Queue now exists, very useful but now I need to go back and remove all my condition variables. Unfortunately means python 3.13 though. Seems ubuntu 25 already uses it but earlier versions requires installation.

[[work_log_jackson/2026-03-18|prev]] [[work_log_jackson/2026-03-20|next]]
