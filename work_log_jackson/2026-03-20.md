## Log
- 25 MiB/s with cp437 encoding
- 385 without. Unfortunately seems like it's an encoding issue as opposed to network, but the good news is this does mean there is room for 15x optimization. This was using the raw async server, I still need to test the sync/asnc adapter.
- Adapter is slightly after than without
- 131072 bytes seems to be about optimal chunk size - I think this is about the bitstream size, so will just do one bitstream per chunk and this will make implementation easier.
- Switched to using msgpack instead of json for serialization. If this makes a difference I can focus on further improvement in this area. I suspect protobuf may be faster, it uses concrete schemas and requires more configuration though. My hope is with the concrete schema it will be much faster for file transfer. If this improves I'll look into protobuf, but it's not a direct drop in replacement. The best method would probably be metadata at the head and all the files making up the rest, with the metadata including start and end byte positions for each file.
- Added utils.Queue to prep for cv removal
## Next
- [ ]

[[work_log_jackson/2026-03-19|prev]] [[work_log_jackson/2026-03-23|next]]
