## Log
- Improved bitstreamevolution docker build time
- Separate images for local and pip icefarm installation
- Switched from docker run to compose, workspace automatically mounted
	- Using compose has the side effect that bitstreamevolution gets to shut down gracefully more often, so devices are more reliably unreserved after an interupt
- Config path able to be changed in container, set as CONFIG_PATH envvar
- Updated readme to reflect changes
	- More info in overview
	- At some point I think we should move the configuration options to a separate doc, also probably move information about local installation (without docker) outside readme to dev guide
- Added info about brltty in icefarm readme
- pico color in readme
- Add docker container logs to readme
- Reboot button works now, able to reboot during evaluations without interrupting ongoing experiment for the most part. If the device is sent bitstreams (or other data) during the reboot that is not going to be received though. 
	- This should be fixable on the batchclient by just not sending bitstreams until the new initialized event is received. Should only occur when using multiple devices.
## Next
- Make batchclient gracefully handle devices that are rebooting by not sending any new bitstreams
- Review stability changes, ensure that client has failsafe for detecting worker timeouts instead of only relying on control notification
- Add serial configuration
- Fix db connection in workflow tests
- Dynamic worker ip
- Update readme with other changes

[[work_log_jackson/2026-03-06|prev]] [[work_log_jackson/2026-03-10|next]]
