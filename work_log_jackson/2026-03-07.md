## Log
- websocket queuing thoughts 
	- Time based sending works for now but is not completely optimal for large populations. Problem is that the client uses the evaluations sent back to know when the worker needs new bitstreams rather than the worker requesting them directly
	- Probably want to add additional parm for when to flush at non zero. On the client this will be determined as a constant of the batch size. 
- Added websocked queuing options
- Looks like the bitstreamevolution buffer parameter in the config was actually setting the batchsize instead, oops. Should not make a difference in performance but might have caused issues with values above 50, since that's what the max websocket transfer size is set at
	- Fixed this, added additional configuration options relating to how many circuits are buffered, when results are sent back, and how many circuits are sent per websocket packet
	- Added new configuration options to bitstreamevolution that uses these
## Next
- Investigate whether rlocks are working properly with pyudev
- Dynamic ip for worker, add to readme
- Add new config options to readme

[[work_log_jackson/2026-03-06|prev]] [[work_log_jackson/2026-03-08|next]]
