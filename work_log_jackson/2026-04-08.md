## Log
- IO tile configuration
	- 1khz_generated pin 27 18, 31
		- pin on iob1
		- pintype 0, 3, and 4 enabled for iob1
		- ioctrl ren_0
			- ren bits are on different io tiles, number does not match cell, in  chipdb
		- ioctrl cf_bit_39 (cf_bit_34 is equiv for cell 0)

![[Screenshot 2026-04-08 at 23-06-43 ICE40 layout viewer.png]]

- Io pins are now configured, able to for the most part generate for multiple circuit areas. It's possible for net collisions to happen currently, but I need to do some further testing before addressing this as the local net naming makes it more complicated. For now I can just use the viewer to confirm that no collisions happened.
- Pretty simple to use, specify a pin, destination tile, optionally a specific destination net, and set of tiles to avoid that are in the genome.
- As far as I'm aware adding the representation to bitstreamevo is the only thing left to do before trying evolution. Still a good amount of things need to happen for running it to actually be accessible though. 
- Able to get pulses using representation with old seed layout. Still need to set up stuff for multiple genomes in one evaluation. I have something to do it with Thomas's representation already but I don't remember if the design will be able to be reused.
## Next
- Create icebox patch for use as a module
- Use singleton for repr configuration
- Figure out speed issue for repr?
	- For full size circuit:
		- 0.13s write
		- 0.05s mutate
		- 0.02s crossver
		- ~0.2s total
		- About 14 seconds for initialization - this is currently per circuit but should be able to be reduced to total
	- This is fine for pulse count but won't work for varmax
	- Really don't want to have repr in not python
- Allow pulsecount firmware pins to be configured
	- Need to modify general firmware to allow for args
	- I think Brooklyn improved the design at some point so also need to incorporate changes
- Old stuff
	- Dynamic worker ip
	- Stop procrastinating on tests
	- Add a limit to how many times a device can reboot without receiving any results. Currently if a device reboots but an issue persists somehow, it will get stuck in a loop
	- Handle external reservation ends gracefully like how failed devices are
	- Dynamic ip, I think with docker its probably just fine to use hostname -I but I need to add a note in the readme to make it clear how to handle when it fails
	- Add reboot event
	- Invalidate generation results from device after reboot? This is really inefficient but I don't see a way to guarantee issues don't happen without the message system overhaul
	- Considering moving to using exceptions
		- I've been doing a lot of stuff more c-style around this, since so much can go wrong it seems much cleaner to use if (stmt) rather than having try:except around every line. It's working well since I am aware of this, but I recognize that this not standard for python. 

[[work_log_jackson/2026-04-07|prev]] [[work_log_jackson/2026-04-09|next]]
- [ ] 